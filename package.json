{
  "name": "audrownashpodcast.com-v3",
  "version": "1.0.0",
  "packageManager": "npm@10.9.2",
  "main": "index.js",
  "scripts": {
    "build": "turbo run build",
    "check-types": "turbo run check-types",
    "dev": "turbo run dev",
    "test": "turbo run test"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
